const cfg=window.SOUNDDECK_CONFIG||{};const hasSupabase=cfg.SUPABASE_URL&&cfg.SUPABASE_URL.startsWith('http')&&cfg.SUPABASE_PUBLISHABLE_KEY&&cfg.SUPABASE_PUBLISHABLE_KEY.startsWith('sb_');const sb=hasSupabase?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_PUBLISHABLE_KEY):null;const state={sounds:[],category:'Alle',search:'',favorites:new Set(JSON.parse(localStorage.getItem('sounddeck:favorites')||'[]')),players:new Map(),boardMode:false,boardIds:[]};const grid=document.querySelector('#grid'),categoriesEl=document.querySelector('#categories'),searchEl=document.querySelector('#search'),selectionButton=document.querySelector('#selectionButton'),selectionCount=document.querySelector('#selectionCount'),panel=document.querySelector('#selectionPanel'),closePanel=document.querySelector('#closePanel'),selectionList=document.querySelector('#selectionList'),clearSelection=document.querySelector('#clearSelection'),copyBoardLink=document.querySelector('#copyBoardLink'),copyHint=document.querySelector('#copyHint'),boardNotice=document.querySelector('#boardNotice');async function boot(){const params=new URLSearchParams(location.search);const board=params.get('board');if(board){state.boardMode=true;state.boardIds=board.split(',').map(x=>x.trim()).filter(Boolean)}state.sounds=await loadSounds();renderCategories();renderSounds();renderSelectionCount();renderBoardNotice()}async function loadSounds(){if(sb){const{data,error}=await sb.from('sounds').select('*').order('created_at',{ascending:false});if(!error&&data&&data.length){return data.map(s=>({id:s.id,title:s.name,category:s.category,soundUrl:s.audio_url,imageUrl:s.image_url}))}}const r=await fetch('data/sounds.json');const d=await r.json();return d.sounds||[]}function saveFavorites(){localStorage.setItem('sounddeck:favorites',JSON.stringify([...state.favorites]))}function visibleSounds(){const q=state.search.trim().toLowerCase();return state.sounds.filter(sound=>(!state.boardMode||state.boardIds.includes(sound.id))&&(state.category==='Alle'||sound.category===state.category)&&(!q||sound.title.toLowerCase().includes(q)||sound.category.toLowerCase().includes(q)))}function renderBoardNotice(){if(!state.boardMode){boardNotice.classList.add('hidden');return}boardNotice.classList.remove('hidden');boardNotice.textContent=`Geteiltes Board mit ${state.boardIds.length} Sound${state.boardIds.length===1?'':'s'}.`}function renderCategories(){const base=state.boardMode?state.sounds.filter(s=>state.boardIds.includes(s.id)):state.sounds;const cats=['Alle',...Array.from(new Set(base.map(s=>s.category))).sort()];if(!cats.includes(state.category))state.category='Alle';categoriesEl.innerHTML='';cats.forEach(category=>{const b=document.createElement('button');b.className=`chip ${state.category===category?'active':''}`;b.type='button';b.textContent=category;b.onclick=()=>{state.category=category;renderCategories();renderSounds()};categoriesEl.appendChild(b)})}function playerFor(sound){if(!state.players.has(sound.id)){const audio=new Audio(sound.soundUrl);audio.preload='auto';state.players.set(sound.id,{audio,playing:false})}return state.players.get(sound.id)}function renderSounds(){const sounds=visibleSounds();grid.innerHTML='';if(!sounds.length){grid.innerHTML='<div class="empty">Keine Sounds gefunden.</div>';return}sounds.forEach(sound=>{const player=playerFor(sound);const card=document.createElement('article');card.className='card';card.innerHTML=`<button class="favorite ${state.favorites.has(sound.id)?'active':''}" type="button">★</button><button class="sound-button ${player.playing?'playing':''}" type="button"><img src="${esc(sound.imageUrl)}" alt="" draggable="false" /></button><p class="sound-title">${esc(sound.title)}</p><p class="sound-category">${esc(sound.category)}</p><div class="controls"><button class="stop" type="button">Stop</button></div>`;const play=card.querySelector('.sound-button'),fav=card.querySelector('.favorite'),stop=card.querySelector('.stop');player.audio.onended=()=>{player.playing=false;renderSounds()};play.onclick=async()=>{player.audio.currentTime=0;await player.audio.play();player.playing=true;renderSounds()};stop.onclick=()=>{player.audio.pause();player.audio.currentTime=0;player.playing=false;renderSounds()};fav.onclick=()=>{state.favorites.has(sound.id)?state.favorites.delete(sound.id):state.favorites.add(sound.id);saveFavorites();renderSelectionCount();renderSounds()};grid.appendChild(card)})}function renderSelectionCount(){selectionCount.textContent=String(state.favorites.size)}function renderPanel(){const selected=state.sounds.filter(s=>state.favorites.has(s.id));selectionList.innerHTML='';copyHint.textContent='';if(!selected.length){selectionList.innerHTML='<div class="empty">Noch keine Sounds ausgewählt.</div>';return}selected.forEach(sound=>{const item=document.createElement('div');item.className='selection-item';item.innerHTML=`<span>${esc(sound.title)}</span><button type="button">Entfernen</button>`;item.querySelector('button').onclick=()=>{state.favorites.delete(sound.id);saveFavorites();renderSelectionCount();renderPanel();renderSounds()};selectionList.appendChild(item)})}selectionButton.onclick=()=>{panel.classList.remove('hidden');renderPanel()};closePanel.onclick=()=>panel.classList.add('hidden');panel.onclick=e=>{if(e.target===panel)panel.classList.add('hidden')};clearSelection.onclick=()=>{state.favorites.clear();saveFavorites();renderSelectionCount();renderPanel();renderSounds()};copyBoardLink.onclick=async()=>{const ids=[...state.favorites];if(!ids.length){copyHint.textContent='Wähle zuerst mindestens einen Sound aus.';return}const url=`${location.origin}${location.pathname}?board=${ids.join(',')}`;await navigator.clipboard.writeText(url);copyHint.textContent='Board-Link wurde kopiert.'};searchEl.oninput=e=>{state.search=e.target.value;renderSounds()};function esc(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}boot().catch(e=>{grid.innerHTML=`<div class="empty">Fehler beim Laden: ${esc(e.message)}</div>`});
